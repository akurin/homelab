apiVersion: v1
kind: Service
metadata:
  name: {{ include "hobby-cluster.fullname" . }}
  labels:
    {{- include "hobby-cluster.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hobby-cluster.selectorLabels" . | nindent 4 }}
