wallabag is deploying to {{ .Values.domainName }}

First boot runs the installer automatically (SQLite database, created on the persistence
volume). This can take a few minutes on a small node — follow it with:

  kubectl -n {{ .Release.Namespace }} logs -f deploy/{{ include "wallabag.fullname" . }}
{{ if .Values.ingress.enabled }}
Published at {{ .Values.domainName }}. Public registration is disabled
(userRegistration: {{ .Values.userRegistration }}), so the admin account created at install
time is the only one.
{{- else }}
The ingress is NOT enabled, so this is only reachable inside the cluster.

The installer creates its admin account non-interactively, which means it currently has
upstream's defaults — username 'wallabag', password 'wallabag', ROLE_SUPER_ADMIN. Do not
publish it in this state. install-wallabag.sh handles that: it changes the password and only
then creates the ingress. If you got here by running helm directly, either finish with

  ./install-wallabag.sh

or change the password yourself before enabling the ingress:

  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "wallabag.fullname" . }} 8080:80
{{- end }}
