apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "leo-journal.fullname" . }}-test-connection"
  labels:
    {{- include "leo-journal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "leo-journal.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
