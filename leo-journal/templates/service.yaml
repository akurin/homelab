apiVersion: v1
kind: Service
metadata:
  name: {{ include "leo-journal.fullname" . }}
  labels:
    {{- include "leo-journal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "leo-journal.selectorLabels" . | nindent 4 }}
