---
- name: Install open-iscsi
  ansible.builtin.apt:
    name: open-iscsi
    update_cache: true

- name: Enable and start iscsid
  ansible.builtin.service:
    name: iscsid
    state: started
    enabled: true
