xray:
  vars:
    ansible_user: root
    grafana_alloy_collector_attrs:
      reverse_proxy: caddy
    xray_xhttp_port_internal: 10000

  hosts:
    ru_firstvds:
      ansible_host: 188.120.246.74
      grafana_repo_mirror: yandex
      domain: clumsypanda.mooo.com
      dns: freedns
      #      cdn_domain: 3v6ocwf108.cdn.twcstorage.ru
      #      server_ip: 151.236.89.224
      next_address: quietotter.mooo.com
      xray_xhttp_extra_settings: &xray_xhttp_extra_settings
      #        uplinkHTTPMethod: GET
      #        xPaddingObfsMode: true
      #        xPaddingPlacement: header
      #        xPaddingHeader: b3
      #        xPaddingMethod: tokenish
      #        sessionPlacement: query
      #        sessionKey: api_key
      #        seqPlacement: query
      #      next_address_xray_xhttp_extra_settings: *xray_xhttp_extra_settings
      warp_outbound_enabled: false
      local_exit_rules:
        - type: field
          domain: [ "domain:ru", "domain:xn--p1ai" ]
          outboundTag: direct
    #        - type: field
    #          ip: [ "geoip:ru" ]
    #          outboundTag: direct

    dk_webdock:
      ansible_host: 92.113.145.63
      domain: quietotter.mooo.com
      dns: freedns
      cdn_domain: c0gziaho6h.cdn.twcstorage.ru
      server_ip: 151.236.89.224
      xray_xhttp_extra_settings:
        uplinkHTTPMethod: GET
        xPaddingObfsMode: true
        xPaddingPlacement: header
        xPaddingHeader: x-b3-traceid
        xPaddingMethod: tokenish
        sessionIDPlacement: path
        sessionIDTable: Base62
        sessionIDLength: 15
        seqPlacement: header
        seqKey: x-b3-spanid
      warp_outbound_enabled: true

    nl_llhost:
      ansible_host: 45.85.117.151
      domain: brisklizard.mooo.com
      dns: freedns
      warp_outbound_enabled: true

  children:
    xray_publish:
      hosts:
        ru_firstvds:
