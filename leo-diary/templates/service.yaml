apiVersion: v1
kind: Service
metadata:
  name: {{ include "leo-diary.fullname" . }}
  labels:
    {{- include "leo-diary.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "leo-diary.selectorLabels" . | nindent 4 }}
