apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "leo-diary.fullname" . }}-test-connection"
  labels:
    {{- include "leo-diary.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "leo-diary.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
