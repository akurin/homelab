apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "leo-journal-next.fullname" . }}-test-connection"
  labels:
    {{- include "leo-journal-next.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "leo-journal-next.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
