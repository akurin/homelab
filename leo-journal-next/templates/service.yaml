apiVersion: v1
kind: Service
metadata:
  name: {{ include "leo-journal-next.fullname" . }}
  labels:
    {{- include "leo-journal-next.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "leo-journal-next.selectorLabels" . | nindent 4 }}
